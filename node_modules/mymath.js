module.exports = function () {
  var sum = 0;
  i = 0,
  args = arguments,
  l = args.length;
  while (i < l) {
    sum += args[i++];
  }

  // console.log(module.exports === exports);
  return sum;
}
